# Changelog

## 1.1.0

- 考试资源新增答题卡 Word 槽位，支持 `.doc` 与 `.docx`，并保留答题卡专用命名和 ZIP 目录。
- 校历改为可视化月历，可点选单日编辑，也可在日历上选择起止日期后批量标注。
- 日期类型增加颜色提示，并在日历中直接显示名称、可用课时和临时课时调整。
- 优化页面背景、卡片、按钮、表单、移动端布局和键盘焦点样式。

## 1.0.0 — Phase 11

- 延期课次可预览后续顺延及原计划对比，确认后新增计划版本，并保留完成记录及旧版本。
- 使用所提供九年级物理工作计划的周内容和课时验证跨月排课及 Excel 投影。
- 增加覆盖完整业务闭环的集成测试和离线 PWA 预缓存。
- 备份纳入全局文件命名设置，并加强计划快照与全局设置校验。
- 项目概览显示本周教学、计划与实际进度、下次考试及本地容量估算。

## 0.10.0 — Phase 10

- 支持导出完整项目 ZIP，包含结构化记录、附件原始字节、版本信息和 SHA-256 清单。
- 导入前校验格式、关联、附件长度及哈希；确认后以新 ID 在单个事务中恢复项目。
- 增加 Dexie v3 项目索引迁移，修复考试等记录按项目查询可能遗漏的问题。
- 覆盖删除后恢复、附件篡改拒绝和同学期冲突回滚测试。

## 0.9.0 — Phase 9

- 按九年级物理样本生成周计划预览和 XLSX，周日为首列，跨月周次拆行并合并相应单元格。
- 支持周备注、跨周检测说明和物理专训值班信息，并标注假期、调休与考试日期。
- 校验样本对应的 24 个数据行、21 个教学周及 A4 横向打印设置。

## 0.8.0 — Phase 8

- 可基于往届项目建立新学期，选择复制任务、预计课时、考试节点及可选校历/课表。
- 新项目重新生成本届日期，不复制旧实际记录、附件或默认人员安排。
- 任务编辑时显示往届计划与实际课时参考，可采用实际课时。

## 0.7.0 — Phase 7

- 支持考试与命题/审题人员、六类标准附件槽位、文件格式校验及关联教学任务。
- 支持单文件下载、动态命名、完整考试 ZIP 和可编辑命名模板。
- 增加跨学期考试资源搜索，普通列表不读取附件 Blob。

## 0.6.0 — Phase 6

- 支持逐课记录完成、部分完成、延期和取消、实际日期/课时、原因及反思。
- 实际记录独立于原计划，首次写入和再次修改都产生 ChangeLog。
- IndexedDB 升级至版本 2，为实际教学记录增加项目索引。

## 0.5.0 — Phase 5

- 增加排课草案预览、冲突提示、周计划和日历视图。
- 用户确认后保存新的计划版本；旧版本保留课次、任务标题和类型快照。
- 草案确认前验证输入是否变化，避免把过期预览写入数据库。

## 0.4.0 — Phase 4

- 增加独立、确定性的排课引擎，支持固定日期/周次、调休和不可拆分任务。
- 返回未排课时与明确的冲突说明，不静默丢弃任务。
- 覆盖规格书列出的 7 个排课场景及输入校验测试。

## 0.3.0 — Phase 3

- 支持教学任务队列的新增、编辑、删除、拖拽和键盘排序。
- 支持任务类型、预计课时、固定日期/周次及课时拆分规则。
- 记录任务修改与排序日志，阻止删除有关联记录的任务，并提供最近一次危险操作的撤销。

## 0.2.0 — Phase 2

- 支持每周学科课表、逐日校历、节假日与调休、日期范围批量设置。
- 支持指定日期覆盖课时，并显示学期可用课时数。
- 可用课时计算独立于 React，覆盖正常周、假期、调休和临时停课测试。

## 0.1.0 — Phase 1

- 建立 React、TypeScript、Vite 和版本化 Dexie 数据库。
- 支持本地学期项目的新建、查看、编辑和确认删除。
- 创建项目时生成逐日校历；修改日期时保留范围内已有校历信息。
- 建立基础测试、构建与 CI。
