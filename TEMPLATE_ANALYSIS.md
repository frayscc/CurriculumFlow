# 现有备课组工作计划 XLSX 分析

来源：[（九年级物理学科）2026-2027学年第一学期备课组工作计划.xlsx](（九年级物理学科）2026-2027学年第一学期备课组工作计划.xlsx)。只读检查，未修改原文件。分析基于单元格、合并、样式和打印设置；导出时仍需在 Excel 或兼容应用中核对打印效果。

## 工作表与日期布局

- 仅有 `Sheet1`，使用范围 `A1:N25`。第 1 行是表头，第 2–25 行为 24 个日历周分段，覆盖 2026-09-01 至 2027-01-23，共 21 个教学周。一周跨月时拆成两行。
- `A` 月份；`B` 中文周次（一、二、…、二一）；`C:I` 为**日、一、二、三、四、五、六**，即周日至周六的日号。第 1 周从 9 月 1 日周二开始，前面的日期留空。
- `J` 工作安排；`K` 课时；`L` 备注；`M` 单元检测；`N` 物理专训。`J` 以 `；` 分隔任务，常用 `13.1-13.2` 等章节压缩文本。`K` 是每周课时合计，第 21 周显式写 0。
- 月份合并：`A2:A6`（9 月）、`A7:A11`（10 月）、`A12:A16`（11 月）、`A17:A21`（12 月）、`A22:A25`（1 月）。跨月周次合并：`B6:B7`（第 5 周）、`B16:B17`（第 14 周）、`B21:B22`（第 18 周）。这三周的 `J/K` 也分别跨两行合并。
- `L/M` 有多周合并，如 `L5:L8`、`M5:M9`、`M15:M18`、`M19:M23`。这表示跨周提示或资源安排，不能把合并内容只归到首行。

## 业务内容映射

| 原表 | 观察到的内容 | 系统中的来源 |
| --- | --- | --- |
| `A:I` | 月份、周次、每日日期 | 项目日期、校历、排课周分段 |
| `J` | 章节、复习、检测、讲评 | 教学任务和计划课次，显示时压缩章节号 |
| `K` | 周课时 0–4，跨月周只出现一次 | 计划课次计数 |
| `L` | 开学考、假期、补班、家长会、期中/期末考 | 校历事件及跨周计划注记 |
| `M` | 章节检测、复习卷套数、命题与审题人员 | 考试元数据与跨周考务注记 |
| `N` | 每个日历分段行的专训负责人或“无” | 按起止日期记录的专项安排 |

`M12` 明确区分期中考命题人与审题人。`M10:M11` 的“期中复习卷2套”和 `M19:M23` 的“期末复习卷4套”是跨周考务安排，不应自动拆成多场考试。`N2:N23` 逐分段填写负责人；第 5、14、18 周跨月的两个分段也可能不同，因此每周单一负责人字段会丢数据。

`D4` 的 9 月 14 日、`G12:H12` 的 11 月 5–6 日、`G24:H24` 的 1 月 14–15 日是红字日期，与备注中的考试日期吻合。`C5` 的 9 月 20 日和 `I8` 的 10 月 10 日为黄色填充，与备注的补班吻合。橙色日期主要是周末和假期。颜色是模板表达，不能单凭颜色自动导入为确定的校历事件。

## 样式与打印

- 表头和正文主要用宋体 11 磅。表头加粗居中；日期与课时居中；`J/M` 长文本左对齐并自动换行。单元格多为四边细线。
- 普通日期白底；休息/假期常用浅橙色 `#FABF8F`；补班黄色 `#FFFF00`；考试日期红字 `#FF0000`。导出时应同时保留文字说明。
- 列宽：`A` 约 5.66、`C` 4、`J` 约 64.66、`K` 约 5.66、`L` 约 25.33、`M` 约 53.33、`N` 约 9.33 个 Excel 字符宽度；其余列使用默认宽度。没有逐行设置行高。
- A4 横向；页边距左右各 0.7 英寸、上下各 0.75 英寸。未设打印区域、打印标题行、分页、冻结窗格或页眉页脚；视图缩放 145%。无公式或批注。

## 导出映射与验收

1. 从自然日期生成教学周，再按月边界拆分展示行；同一周的任务和课时只在首分段行显示，并合并跨分段的 `B/J/K`。
2. `C:I` 按周日至周六排列，仅显示当前月份且在项目日期范围内的日号；月份列按连续分段合并。
3. `L/M` 由结构化事件和跨周注记生成，按覆盖行合并；业务数据不存 Excel 单元格坐标。重叠注记需分别显示或提示。
4. `N` 从专项安排的日期区间生成，每个分段保留独立负责人。
5. 用 ExcelJS 仿照字体、边框、列宽、合并和 A4 横向设置。以样本校验 21 周、24 个分段、3 组跨月周合并、日期标记和内容；另做人工打印检查。

## 待确认

- `N` 的“物理专训”是值班/专项工作负责人，还是关联某项教学任务？建议 V1 保存日期区间和负责人，可选关联任务。
- 样本中假期及考试日期可能只是计划草稿；录入为本届正式校历时需用户确认。
